services:
  db:
    container_name: postgres_container
    image: postgres:18.4-alpine3.23
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: abc
      POSTGRES_USER: admin
      POSTGRES_DB: db